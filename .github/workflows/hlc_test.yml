name: hlc test secret env vars
on: push
env:
  USERNAME: ${{ secrets.USERNAME }}

jobs:
  test-hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo hello, world!
      - run: echo $USERNAME
